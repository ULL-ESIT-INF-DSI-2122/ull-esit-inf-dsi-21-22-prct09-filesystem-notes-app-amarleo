import 'mocha';
import {expect} from 'chai';
import {index} from '../src/index';

describe('test', () => {
  describe('', () => {
    it('', () => {
      expect(index()).to.be.true;
    });
  });
});
